version: '3.2'

services:

#================================================================================================
# GOLANG
#================================================================================================
  golang:
    restart: always
    build:
      context: .
    ports:
      - "9101:22"
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DATA_GOLANG}:/go
    environment:
      - GOLANG_VERSION=${GOLANG_VERSION}
      